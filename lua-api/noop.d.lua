---@meta

---Does nothing. Provided for backward compatibility.
function Redraw() end

---Does nothing. Provided for backward compatibility.
function Repaint() end

---Does nothing. SmushClient does not cache IP addresses. Provided for backward compatibility.
function ResetIP() end

---Does nothing. Provided for backward compatibility.
---@param colour integer|string
function SetFrameBackgroundColour(colour) end

---Does nothing. Provided for backward compatibility.
function utils.reload_global_prefs() end

---Does nothing. Provided for backward compatibility.
---@param name string
function utils.sendtofront(name) end
